/**
 * @file admin/login/page.tsx
 * @description 管理側ログイン画面（/admin/login URL用）
 *
 * URLパス /admin/login でログイン画面を表示する。
 * 実際のUIはsrc/app/(auth)/login/page.tsxで定義された
 * LoginPageコンポーネントを再利用する。
 */

export { default } from '../../(auth)/login/page';
